/*******************************************
 * Project: ExitingEarly
 * Programmer: Matthew Taylor
 * Date: 2020/04/20
 * Program: ExitingEarly.java
 *******************************************/
package forloops;

import java.util.Scanner;

public class ForLoops
{
    public static void main(String[] args)
    {
        // 1.
/*
        Scanner scanN = new Scanner(System.in);
        Scanner scanS = new Scanner(System.in);
        String pet, layout;
        int printCount = 0;
        
        // Input pet
        System.out.print("Please type your favorite pet: ");
        pet = scanS.nextLine();
        
        // Input printCount
        boolean invalidNumber;
        do
        {
            
            System.out.print("\nPlease type number of times you would like to print: ");
            printCount = scanN.nextInt();
            invalidNumber = printCount < 0;
            if (invalidNumber)
            {
                System.out.print("\nInvalid number - Number must be positive number");    
            }
        }
        while (invalidNumber);
        
        // Input layout and validation
        boolean invalidLayout;
        do
        {
            System.out.print("\nWould you like Vertical or Horizontal: ");
            layout = scanS.nextLine();
            invalidLayout = !layout.equalsIgnoreCase("Horizontal") &&
                            !layout.equalsIgnoreCase("Vertical");
            if (invalidLayout)
            {
                System.out.println("Invalid layout. Must be Horizontal or Vertical");
            }
        }
        while (invalidLayout);
        
        // Print report using data
        if (layout.equalsIgnoreCase("Vertical"))
        {
           for (int i = 0; i < printCount; i++)
            {
                System.out.println(pet);
            } 
        } 
        else if (layout.equalsIgnoreCase("Horizontal"))
        {
            for (int i = 0; i < printCount; i++)
        {
            System.out.print(pet + " ");
        }
        }
*/

        // 2.
    }
}
